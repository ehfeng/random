# Random Postgres Extension

Inspired by [Python's random module](https://docs.python.org/3/library/random.html)

## Usage

```sql
select random.uniform(1, 6);
```

## Install

Clone repo and `make install`

Within database, `create extension faker;`
